# Port the app binds to (127.0.0.1 only — nginx reverse-proxies in front).
# Default: 3700
PORT=3700

# Node environment. Affects error verbosity and some library behaviors.
# Values: development | production
NODE_ENV=development
